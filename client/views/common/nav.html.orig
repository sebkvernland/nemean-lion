<template name="nav">
<<<<<<< Updated upstream
    <nav class="mainMenu">
        {{#each menuItems}}
        <a class="menuitem {{#if menu}}isParent{{/if}}"
           href="{{link}}">{{title}}</a>
        {{#if menu}}
            <span class="submenu">
                {{#each menu}}
                <a class="submenuitem" href="{{link}}">{{title}}</a>
                {{/each}}
            </span>
        {{/if}}
        {{/each}}
    </nav>
=======
        <nav class="navbar-collapse">
        	{{#each menuItems}}
            <a class="menuitem" href="{{link}}">{{title}}</a>
            {{/each}}
        </nav>
>>>>>>> Stashed changes
</template>